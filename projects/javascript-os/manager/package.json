{
  "name": "manager",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node index.js",
    "postinstall": "cd ../server && pnpm install && cd ../browser && pnpm install"
  },
  "devDependencies": {
    "manager": "link:",
    "server": "link:..\\server"
  }
}
