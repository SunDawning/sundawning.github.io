{
  "name": "manager",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "nodemon index.js --watch index.js --watch package.json",
    "postinstall": "cd ../server && pnpm install && cd ../browser && pnpm install"
  },
  "devDependencies": {
    "manager": "link:",
    "nodemon": "^2.0.16",
    "portfinder": "^1.0.28",
    "server": "link:..\\server"
  }
}
