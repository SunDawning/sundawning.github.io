{
  "scripts": {
    "dev": "npm-run-all --parallel build serve",
    "build": "esbuild ./src/index.tsx --bundle --outfile=./dist/index.js --watch",
    "serve": "http-server ./dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.11",
    "http-server": "^14.1.1",
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5"
  }
}
